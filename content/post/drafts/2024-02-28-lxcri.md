---
title: 'MicroRack v1 - un rack compact pour les Dell Micro de mon homelab'
authors:
  - zwindler
type: post
date: 2024-02-15T17:00:00+02:00
draft: true
url: /2024/02/15/microrack-v1-pour-dell-micro/
image: /2024/02/microrack.png
categories:
  - DIY
  - Matériel
tags:
  - aaa

---

## Il ne savait pas que c'était complètement stupide, alors il l'a fait

Que mensonge... Bien sûr, je sais très bien que c'est débile. 

Mais à force de trifouiller les entrailles de Kubernetes depuis quelques mois, l'idée m'a fait rire.

Si vous suivez le blog, vous savez que j'utilise beaucoup Proxmox VE (les articles qui attirent le plus sur le blog sont d'ailleurs des articles sur cette technologie de virtualisation).

Proxmox VE, c'est très cool ; on peut faire des VMs (QEMU) avec, mais aussi, si on a pas de VT-x ou si qu'on veut des "lightweight VMs" (terme dont on a abusé à outrance), on peut installer des OS complets dans des containers avec LXC.

Mais au delà de ces deux solutions, les devs du projet Proxmox VE sont assez rigides (et pas que là dessus). C'est d'ailleurs pour ça que j'ai posté quelques "hacks" pour contourner les limitations de Proxmox, notamment cet article où j'explique comment lancer des containers Docker (via LXC) dans Proxmox VE (normalement pas possible).

Et si on allait encore plus loin dans le hack idiot ? Si, non seulement on exécutait des containers Docker dans Proxmox VE, mais qu'EN PLUS, on transformait nos hôtes Proxmox VE en Nodes Kubernetes ???

## Tu pousses le bouchon un peu loin Maurice !

En théorie c'est possible, Kubernetes permet beaucoup de choses, notamment la possibilité d'avoir plusieurs "container runtime". Peu importe quelle technologie de containérisation (whatever that means), du moment que vous avez un brique logicielle qui permet au kubelet (l'agent de Kubernetes) de discuter avec le runtime de manière standardisée, c'est OK. 

C'est d'ailleurs pour ça qu'on peut faire du Kubernetes avec des VM libvirt (kubevirt),  des microVMs (firecracker, kata containers), et tout un tas de container runtimes plus classiques (docker, containerd, ...).

Du coup, Proxmox VE supportant 2 technos de virtualisation différentes (qemu et LXC), il faut choisir. Et comme j'aime bien LXC et que j'ai déjà expérimenté sur le hack Docker => LXC sur Proxmox VE, la question était vite répondue.

## On s'y prend comment ?

Tout la difficulté de l'exercice est de trouver comment rendre LXC comme CRI compatible. Et on a de la chance, Linux Containers, la fondation qui chapeaute LXC, Incus (ex LXD, "refermé" par Canonical), a écrit il y a 3 ans un CRI pour LXC appelé lxcri.

On va donc l'installer sur notre Proxmox VE comme des gorets et voir ce que ça donne.

> lxcri is a wrapper around LXC which can be used as a drop-in container runtime replacement for use by CRI-O.

lxcri s'appuie donc sur crio.

https://cri-o.io/

On commence donc par installer cri-o

https://github.com/cri-o/cri-o/blob/main/install.md

Note : mon serveur en Proxmox VE 8 utilise Debian 12 comme OS de base. On devrait donc se baser la dessus pour la variable $OS (comme l'indique la doc). Cependant, à l'heure où j'écris ces lignes, il se trouve que le répo https://download.opensuse.org/repositories/devel:/kubic:/libcontainers:/stable:/cri-o/ n'a pas le PATH Debian_12

https://github.com/cri-o/cri-o/issues/7657
https://kubernetes.io/blog/2023/10/10/cri-o-community-package-infrastructure/

Idem, il n'y a pas de version 1.29 disponible. On doit donc se contenter de la 1.28

```bash
root@par01:~# export OS=Debian_11
root@par01:~# export VERSION=1.28

root@par01:~# echo "deb [signed-by=/usr/share/keyrings/libcontainers-archive-keyring.gpg] https://download.opensuse.org/repositories/devel:/kubic:/libcontainers:/stable/$OS/ /" > /etc/apt/sources.list.d/devel:kubic:libcontainers:stable.list
root@par01:~# echo "deb [signed-by=/usr/share/keyrings/libcontainers-crio-archive-keyring.gpg] https://download.opensuse.org/repositories/devel:/kubic:/libcontainers:/stable:/cri-o:/$VERSION/$OS/ /" > /etc/apt/sources.list.d/devel:kubic:libcontainers:stable:cri-o:$VERSION.list

root@par01:~# mkdir -p /usr/share/keyrings
root@par01:~# curl -L https://download.opensuse.org/repositories/devel:/kubic:/libcontainers:/stable/$OS/Release.key | gpg --dearmor -o /usr/share/keyrings/libcontainers-archive-keyring.gpg
root@par01:~# curl -L https://download.opensuse.org/repositories/devel:/kubic:/libcontainers:/stable:/cri-o:/$VERSION/$OS/Release.key | gpg --dearmor -o /usr/share/keyrings/libcontainers-crio-archive-keyring.gpg

root@par01:~# apt-get update
root@par01:~# apt-get install cri-o cri-o-runc
```

A partir de là, on a crio et crio-runc. On pourrait le configurer mais on n'utiliserait pas LXC en tant que runtime et ce n'est pas le but de ce hack.

C'est donc à ce moment là qu'on bascule sur 
https://github.com/lxc/lxcri/blob/main/doc/setup.md

## Stoooooop !

Avant de faire quoique ce soit qu'on pourrait regretter, on se pose.

Ce que la doc ne dit pas, c'est qu'il faut préalablement buidler le binaire `lxcri` et l'installer dans le /usr/local de notre serveur Proxmox VE. Il n'y a pas de binaire précompilé sur les releases du projet :grimacing.

Et pour que ce soit encore plus fun, le processus de build passer par un Dockerfile, ce qui est rigolo puisqu'on a pas Docker sur notre node proxmox...

J'ai donc essayé de builder lxcri en suivant la commande indiquée sur le Github.

Patatra. Déjà, il manque un "." en fin de commande, mais même avec ça, ça fail misérablement à la compilation...

```
git clone https://github.com/lxc/lxcri.git
cd lxcri
docker buildx build --build-arg installcmd=install_runtime .
```

![](/2024/02/build-lxcri-1.png)

En vrai, c'est pas hyper surprenant... Le projet date d'il y a 3 ans et n'est visiblement plus maintenu...

En allant jeter un oeil au Dockerfile, on se rend vite compte qu'on ne fait que lancer un script (install.sh)... Voilà les fonctions qui nous intéressent :

```bash
add_lxcri() {
        local repo=$LXCRI_GIT_REPO
        local version=$LXCRI_GIT_VERSION
        local tmpdir=${TMPDIR}/lxcri

        git_clone $tmpdir $repo $version

        # lxc installed from source with default installation prefix is prefered
        export PKG_CONFIG_PATH=${INSTALL_PREFIX}/lib/pkgconfig
        make install

        cd
        rm -rf $tmpdir
}

install_runtime_noclean() {
        setup $PKGS_BUILD $PKGS_RUNTIME
        add_lxc
        add_lxcri
}

install_runtime() {
        install_runtime_noclean
        clean
}
```

Quand on appelle docker avec le buildarg installcmd=install_runtime, on lance la fonction install_runtime, qui appelle install_runtime_noclean, qui lance add_lxc puis add_lxcri

En théorie, je n'ai pas besoin de build lxc. J'ai déjà une version fonctionnelle de LXC sur mon Proxmox VE. Je veux juste compiler lxcri. Sauf que pour compiler lxcri, il faut que j'ai les bindings de lxc. Voilà ce qui se passe si je lance juste un `make build` (ce que fait add_lxcri() dans install.sh) :

```bash
go build gopkg.in/lxc/go-lxc.v2:
# pkg-config --cflags  -- lxc
Package lxc was not found in the pkg-config search path.
Perhaps you should add the directory containing `lxc.pc'
to the PKG_CONFIG_PATH environment variable
No package 'lxc' found
pkg-config: exit status 1
make: *** [Makefile:42 : lxcri] Erreur 1
```
Après toute 2 petites mises à jour dans le projet (voir la PR en cours), j'arrive à builder lxcri

```
docker buildx build --build-arg installcmd=install_runtime .
[+] Building 62.8s (8/8) FINISHED                                                                                                       docker:default
 => [internal] load build definition from Dockerfile                                                                                              0.0s
 => => transferring dockerfile: 1.33kB                                                                                                            0.0s
 => [internal] load metadata for docker.io/library/ubuntu:latest                                                                                  0.3s
 => [internal] load .dockerignore                                                                                                                 0.0s
 => => transferring context: 2B                                                                                                                   0.0s
 => [internal] load build context                                                                                                                 0.0s
 => => transferring context: 32B                                                                                                                  0.0s
 => [1/3] FROM docker.io/library/ubuntu:latest@sha256:f9d633ff6640178c2d0525017174a688e2c1aef28f0a0130b26bd5554491f0da                            0.0s
 => CACHED [2/3] COPY install.sh /                                                                                                                0.0s
 => [3/3] RUN /install.sh install_runtime                                                                                                        62.4s
 => exporting to image                                                                                                                            0.0s
 => => exporting layers                                                                                                                           0.0s
 => => writing image sha256:7d91aecbc891c1a90ca05bf0613778edeff77312b2c7bb0dcbc82c4a41b98c83                                                      0.0s
```

Sauf que le binaire est DANS l'image docker... krkrkr

Enfin bon, on s'en sort avec un docker run / docker cp

```bash
docker cp 4c5bc7d1b9dd:/usr/local/bin/lxcri .
for file in lxcri-hook lxcri-hook-builtin lxcri-init lxcri-start; do docker cp 4c5bc7d1b9dd:/usr/local/libexec/lxcri/${file} .; done
ls lxcri*
lxcri  lxcri-hook  lxcri-hook-builtin  lxcri-init  lxcri-start
```

On envoie les binaires sur le serveur Proxmox VE (lxcri dans /usr/local/bin, le reste dans /usr/local/libexec/lxcri) et on peut continuer :

```bash
root@par01:~# /usr/local/bin/lxcri help
NAME:
   lxcri - lxcri is a OCI compliant runtime wrapper for lxc

USAGE:
   lxcri [global options] command [command options] [arguments...]
```

## On reprend crio

On repart donc sur la doc https://github.com/lxc/lxcri/blob/main/doc/setup.md et on configure crio pour qu'il n'utilise pas runc mais lxcri.

```bash
PREFIX=/usr/local
LXCRI_ROOT=/run/lxcri

# environment for `crio config`
export CONTAINER_DEFAULT_RUNTIME=lxcri
export CONTAINER_RUNTIMES=lxcri:${PREFIX}/bin/lxcri:$LXCRI_ROOT
crio config > /etc/crio/crio.conf
```

A noter, la doc d'installation dit de remplacer les PATH de conmon et de pinns (CONTAINER_CONMON et CONTAINER_PINNS_PATH). Ne le faites pas. On va utiliser ceux fournis par crio (dans /usr/bin)

Ca doit nous renvoyer :

```bash
INFO[2024-02-18 21:47:23.698336776+01:00] Starting CRI-O, version: 1.28.2, git: unknown(clean) 
INFO Using default capabilities: CAP_CHOWN, CAP_DAC_OVERRIDE, CAP_FSETID, CAP_FOWNER, CAP_SETGID, CAP_SETUID, CAP_SETPCAP, CAP_NET_BIND_SERVICE, CAP_KILL 
```

Lancer crio

```bash
systemctl enable crio && systemctl start crio
```

A partir de là, on a un serveur disposant d'un CRI *a priori* fonctionnel. On peut donc installer kubelet, puis l'ajouter à un cluster Kubernetes existant

## Prérequis

Commençons par juste installer le kubelet.

```
./kubelet --kubeconfig admin.conf --container-runtime-endpoint --container-runtime-endpoint=unix:///var/run/crio/crio.sock --fail-swap-on=false --cgroup-driver="systemd"
```


https://github.com/lxc/lxcri/issues/39
